# Knowledge MCP

MCP tool definitions expose the canonical manifest and lazy-load the core handlers. Read tools are revision-scoped; mutations are audited and unsupported capabilities return typed errors.
