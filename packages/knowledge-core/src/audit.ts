import { createHash, randomUUID } from "node:crypto";
import type { KnowledgeStore } from "./store.js";
function digest(value: unknown): string { return createHash("sha256").update(JSON.stringify(value)).digest("hex"); }
export class AuditStore { constructor(private readonly store: KnowledgeStore) {} append(input: { capabilityId: string; actorId: string; scopeHash: string; input: unknown; resultCode: string }): string { const previous = this.store.db.prepare("SELECT event_hash FROM knowledge_audit_events ORDER BY seq DESC LIMIT 1").get() as { event_hash: string } | undefined; const eventId = `audit_${randomUUID()}`; const createdAt = new Date().toISOString(); const eventHash = digest({ eventId, previousHash: previous?.event_hash ?? null, capabilityId: input.capabilityId, actorId: input.actorId, scopeHash: input.scopeHash, inputDigest: digest(input.input), resultCode: input.resultCode, createdAt }); this.store.db.prepare("INSERT INTO knowledge_audit_events(event_id,previous_hash,event_hash,capability_id,actor_id,scope_hash,input_digest,result_code,created_at) VALUES (?,?,?,?,?,?,?,?,?)").run(eventId, previous?.event_hash ?? null, eventHash, input.capabilityId, input.actorId, input.scopeHash, digest(input.input), input.resultCode, createdAt); return eventId; } verify(): { ok: boolean; brokenAt?: number } { let previous: string | null = null; for (const row of this.store.db.prepare("SELECT * FROM knowledge_audit_events ORDER BY seq").all() as Array<Record<string,string|number>>) { if ((row.previous_hash ?? null) !== previous) return { ok: false, brokenAt: Number(row.seq) }; previous = String(row.event_hash); } return { ok: true }; } list(input: { scopeHash?: string; from?: string; to?: string; limit?: number } = {}): Array<Record<string, unknown>> { const clauses = ["1=1"]; const params: unknown[] = []; if (input.scopeHash) { clauses.push("scope_hash=?"); params.push(input.scopeHash); } if (input.from) { clauses.push("created_at>=?"); params.push(input.from); } if (input.to) { clauses.push("created_at<=?"); params.push(input.to); } params.push(input.limit ?? 1000); return this.store.db.prepare(`SELECT seq,event_id AS eventId,capability_id AS capabilityId,actor_id AS actorId,scope_hash AS scopeHash,input_digest AS inputDigest,result_code AS resultCode,created_at AS createdAt,event_hash AS eventHash FROM knowledge_audit_events WHERE ${clauses.join(" AND ")} ORDER BY seq LIMIT ?`).all(...params) as Array<Record<string, unknown>>; } export(input: { scopeHash?: string; from?: string; to?: string; limit?: number } = {}): Array<Record<string, unknown>> { return this.list(input); } }
