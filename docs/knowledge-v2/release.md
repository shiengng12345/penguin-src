# Release

Run `pnpm run typecheck`, `pnpm run knowledge:benchmark:universal -- --root=<corpus> --limit=10000 --gate`, `pnpm run knowledge:parity`, `pnpm run knowledge:package-smoke`, `pnpm run knowledge:sbom` and the real-question differential. A green local typecheck alone is not a release approval.
