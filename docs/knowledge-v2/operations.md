# Operations

Routine checks: `penguin status --json`, `penguin coverage --json`, `penguin doctor --json`, `penguin capabilities --json`. Reindex after source changes; use the resident runtime for repeated Wiki queries. Keep artifact exports and the vault backup together. Never delete external indexes before two independent release candidates and an operator-approved rollback rehearsal.
