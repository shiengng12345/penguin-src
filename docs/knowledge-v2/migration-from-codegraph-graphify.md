# Migration from CodeGraph/Graphify

Keep external indexes as differential oracles during rollout. First compare exact/path recall, revision isolation, provenance and surface parity. Disable hooks only after RC1 and RC2 pass, universal recall is 100 percent twice, packaged doctor is healthy twice, rollback passes twice and an operator explicitly approves removal.
