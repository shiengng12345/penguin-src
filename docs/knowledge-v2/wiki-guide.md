# Wiki guide

The Wiki Search tab is the high-frequency entry point. Search is debounced, mode-aware and displays coverage warnings. A result should open the source path/line first, then context, graph, WHY and evidence. Wiki is a view over the same contract, not a second search implementation.
