# Penguin Knowledge — Round 26 Independent Evaluation (Claude Code)

## 0. Evaluator and fresh-process proof

| Item | Value |
| --- | --- |
| Evaluator | Claude Code (CLI), model `claude-opus-5[1m]` (Opus 5, 1M context) |
| Evaluation date | 2026-09-02 |
| Fresh-process proof | New OS Claude Code process. Penguin MCP `initialize` performed by this process: `sessionId 5703228d-5069-40b1-8d69-48b07531f4b3`, `clientConnectedAt 2026-09-02T07:32:45.322Z`. First product call `mcp_health` returned `serverTimeUtc 2026-09-02T07:33:30.175Z` — 45s after connect. No prior Penguin state, report, target, cursor or score was read. |
| Brief read | `docs/quality/index-evaluation-brief.md` (277 lines) + the Round 26 new-session instruction, before any product call. |
| Boundary compliance | Penguin MCP was the only product/data path. No source file, Git command, SQLite access, Penguin CLI, plan, bundle, prior report, or other code-index product was used. Read-only throughout: `knowledge_repository_register`, `knowledge_index`, `knowledge_rebuild`, `knowledge_semantic_control`, note/memory/link/source/evidence writes were **never** invoked (not even with `confirmed:false`). Two non-product host utilities were used and are disclosed: `date -u` for one harness wall-clock stamp, and `jq` to query the `knowledge_capabilities` MCP response after the host spooled that oversized MCP result to a local file. Both operate only on MCP output, not on the corpus. |
| Report | This file only. The Codex report was not read or modified. |

## 1. Runtime identity discovered in this session

| Runtime surface | Session/build/schema/contract/hash | Configured | Loaded | Healthy | Useful | Restart required | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| MCP transport | `sessionId 5703228d-5069-40b1-8d69-48b07531f4b3`, connected `07:32:45.322Z` | yes | yes | yes | yes | no | PASS |
| App/runtime | `appVersion 1.16.0`, `buildId 1.16.0-f5de64b02a83a582`, node `v22.23.1`, darwin/arm64 | `configured:true` | `launcherHealthy:true`, `initializeHealthy:true` | `status:"ok"` | bounded query returned verified hits | `restartRequired:false`, `clientRestartRequired:false`, `runtimeOutdated:false` | PASS |
| Generation parity | `runningBuildId == availableBuildId == 1.16.0-f5de64b02a83a582`, `outdated:false` | — | — | — | — | no | PASS (no old/new mixture) |
| Contract | `contractVersion "2"`, `schemaVersion 18`, `capabilityHash f99fca378bb72ee30313ccc9da98ce4eb353c9b41eff230726186fd16446b67f`, `modelHash 4193e4f88c3f16239a6853400ae2765b4b0903e97c80fcbfd814af24dd2a01e5` | yes | yes | yes | consistent on every response (`diagnostics.capabilityHash`) | no | PASS |
| Persisted index schema | `trust.indexedSchemaVersion 17` vs `trust.schemaVersion 18` (every `knowledge_context` / `knowledge_flow` trust block) | yes | yes | `stale:false`, `alignment:"aligned"` | usable | not signalled | **FAIL (skew)** — index built under schema 17, runtime is 18; disclosed but never reconciled and no remediation offered |
| Native deps | bundled-node, better-sqlite3, sqlite-vec, onnxruntime-node, sharp, `nomic-embed-text-v1.5` manifest+model+tokenizer — all `status:"ready"` with sha256 | yes | yes | yes | embedding runtime present but **no generation exists** | no | PARTIAL |
| Signing | `signing.status:"unsigned"` | — | — | — | — | — | N/A (out of scope per brief §3) |
| DB | `status_panel.db`: `connected:true`, `schemaVersion 18`, `sizeBytes 12,039,758,336`, `walBytes 82,432` | yes | yes | yes | yes | no | PASS |
| Query runtime | `queryRuntime {workers:2, hardTimeoutMs:30000}`, `cursorTtlSeconds 900` | yes | yes | yes | partly — 9 hard timeouts observed | no | **FAIL (reliability)** |

`configured` / `loaded` / `healthy` / `useful` are distinct here: the runtime is configured and loaded, reports healthy, and is *useful* for exact/lexical/graph work — but is **not** useful for `knowledge_endpoints` or for any semantically-blended query (both exceed the hard timeout), and the semantic lane is configured and loaded yet has nothing to serve.

## 2. Dynamic target selection (all discovered in this session)

| Target | Value | MCP discovery chain |
| --- | --- | --- |
| Largest Repo | **FPMS-NT** — `repo_a48ec7fb-5987-47df-9198-06969359cb50`, branch `brazil-v2` (`branch_1d21f868-3252-4b74-8289-8f7c4735247f`), commit `3f0f1984b9e4337668529a13bad5264501729908`, snapshot `snapshot_c7bf7942-2ca8-46c5-9064-db607bd598f7`, admitted **3333** | `index_status(compact)` → `status_panel` (largest `coverage.admitted` among aligned/fresh repos; FPMS has 3141 but is `branch_not_indexed` + `worktree_dirty`) |
| Second Repo | **casino-plus** — `repo_c5cb9c2b-9245-4892-be53-57890e82dc86`, branch `main`, commit `606364ba3c587b0e52031576fd89ca9fba7b5f18`, snapshot `snapshot_fd3c3749-1ced-4f2e-952a-5ec1f1a6925f`, admitted 2222 | `status_panel` |
| Natural Intent | `responsible gaming deposit limit check` (5 words, never adjacent as a phrase in either repo) | derived from `get_architecture(FPMS-NT)` entry points + `knowledge_service_graph` domain vocabulary |
| Primary Symbol | **DepositLimitController** — `node_71666155-b7ab-4ff4-960a-b69ad091aabe`, identityKey `repo_a48ec7fb-…::apps/riskControl/src/antiAddiction/deposit-limit.controller.ts::DepositLimitController` | `knowledge_search` (Natural Intent) hit `hit_934c2bdb1a315a75efaed434` |
| Relation-rich Symbol | **VaultFetcher** — `node_0a4422f9-e8d5-44d0-bf35-ed9369615208`, 213 relation candidates, signal `high fan-in: 10 callers` | `get_architecture(FPMS-NT).hubs` (degree 297) → `knowledge_search(mode:exact)` |
| Primary File | `apps/riskControl/src/antiAddiction/deposit-limit.controller.ts` | `get_node(node_71666155-…).versions[0].filePath` |
| Primary Endpoint | **gRPC riskcontrol.DepositLimitService.CheckPlayerDepositLimit** — `node_20863614-bb72-4b09-b638-6f05b7f7dc70`, handled, ≥4 downstream hops | `knowledge_affected(Primary File).routes` → `knowledge_flow(node_2d8dcacf-…)` root. Secondary (inventory-sourced): `gRPC proposal.ProposalService.ExecuteProposal` `node_663234af-3485-4a1b-976a-e63bfccf719d`, identityKey `grpc::proposal.ProposalService.executeproposal`, from `knowledge_endpoints(FPMS-Proposal-SDK)` |
| Service Pair | **FPMS-NT ↔ FPMS-NT-Payment** (`service:repo_a48ec7fb-…` ↔ `service:repo_670fda10-d25a-4ad5-9dc8-50766fefe986`) | `knowledge_service_graph(FPMS-NT, include_direct_neighbours)` |

Identity hazard recorded during selection: **two distinct repositories share the name `FPMS-NT-Auth-Player`** (`repo_5513e90d-…` at `/Users/shieng/Desktop/Projects/auth` and `repo_f5e835f6-…` at `/Users/shieng/Desktop/Projects/auth-penguin-benchmark`). Name-based scoping of that repo is ambiguous; only repoId disambiguates.

## 3. Q1–Q20 scenario scores

| Scenario | Score /100 | Verdict | Direct MCP evidence | Limitation/failure | Remediation |
| --- | ---: | --- | --- | --- | --- |
| Q1 Fresh runtime identity chain | 82 | PASS with skew | `mcp_health` full identity block; `knowledge_capabilities` echoes same `schemaVersion 18` / `contractVersion 2` / `capabilityHash f99fca…` / `modelHash 4193e4…` / `buildId 1.16.0-f5de64b02a83a582`; `knowledge_semantic_status` → `{"statuses":[]}`; bounded query `search_914c738a1a325c58` returned 8 verified hits in 4919.974 ms | `trust.indexedSchemaVersion 17` ≠ `trust.schemaVersion 18` on every context/flow response; no reconciliation, no remediation, no restart signal. `signing.status:"unsigned"` (out of scope) | Surface an explicit `indexSchemaSkew` field with a typed remediation, or re-index under schema 18 |
| Q2 Capability schema & annotation audit | 84 | PASS | `knowledge_capabilities` (101 capabilities) exposes per-capability `mutating`, `confirmation`, `supportsCursor`, `supportsCompact`, `annotations{readOnlyHint,destructiveHint,idempotentHint,openWorldHint}`, `inputSchemaId`, `outputSchemaId`. **Zero inversions**: all 39 `mutating:true` are `readOnlyHint:false/destructiveHint:true/idempotentHint:false/confirmation:"required"`; all 62 `mutating:false` are `readOnlyHint:true/destructiveHint:false/idempotentHint:true/confirmation:"not_required"`. Observed behaviour matched (read-only calls never prompted, never changed state) | `destructiveHint:true` on purely additive ops (`note.create`, `note.append`, `memory.remember`, `api_doc.draft`, `watch`) is over-broad; `openWorldHint:false` on `cli.install` and `source.sync` is arguably untrue; `mcp_health` has no manifest entry; MCP tool ids diverge from capability ids with no alias map in the manifest | Publish a tool-id↔capability-id alias map in the manifest; split `destructiveHint` from `mutating`; set `openWorldHint:true` for install/sync |
| Q3 Repository ranking & file reconciliation | 85 | PASS | `knowledge_files(FPMS-NT)` returns a `reconciliation` block: `admittedSourceFiles 3333`, `graphParsedFiles 3298`, `sourceAndGraphFiles 3298`, `sourceOnlyFiles 35`, `graphOnlyFiles 0`, `candidateFiles 3333`, three published equations, `reconciles:true`. Verified arithmetic: 3298+35=3333 ✓, 3298+0=3298 ✓, 3333+0=3333 ✓. Paged at limits 5/2/4 → `returnedCount/remainingCount` 5/3328, 2/3331, 4/3329 against `candidateCount 3333`, `totalIsExact:true`, `truncated:true`, cursor emitted each time. Three `source_only` samples: `.dockerignore`, `.github/actions/setup-node/action.yml`, `.github/codex/prompts/review.md` | `get_architecture(FPMS-NT).nodeCounts.file = 3306` vs `graphParsedFiles 3298` — an 8-file delta reconciled nowhere | Add the architecture file-node count to the reconciliation equations, or explain the 8 extra file nodes |
| Q4 Same multi-term query across two repos | 86 | PASS | 5 calls, MCP-reported `timingsMs.total`: FPMS-NT 4919.974 / 2497.785 / 4792.35; casino-plus 4223.838 / 3254.959. All `queryStatus:"MATCH"`, `scopeApplied:true`. FPMS-NT `candidateCount 964`(limit 8) / `962`(limit 3); casino-plus `480`. Top FPMS-NT identities: `apps/payment/libs/utils/errorCodeConstants.ts:46`, `bank-card-processor.ts:255` (`node_ad7328a6-…`), `payment.module.ts:59`, `top-up-balance.check.ts:7`, `deposit-limit.service.ts:7` (`node_2b4f1ba8-…`), `deposit-limit.controller.ts:21` (`node_71666155-…`). Top casino-plus: `anti-addiction-setting.tsx:141` (`node_73082685-…`), `anti-addiction-change-limit.tsx:36` (`node_cc735f2b-…`). Warning on all: `COVERAGE_INCOMPLETE`. `semantic{requested:false,applied:false,reason:"not_requested"}`. Identical-input pairs returned identical hitIds/order/scores/candidateCount and identical cursor payloads modulo `expiresAt` | `candidateCount` is limit-dependent (964→962 for the same query/scope; 8→7 elsewhere) — a "total" that shrinks when fewer rows are requested, mitigated only by `page.totalIsExact:false`. Latency variance 1.9× on identical input (2497.785 → 4792.35 ms) | Make `candidateCount` limit-invariant or rename it `candidatesScanned` |
| Q5 Exact occurrence continuation | 93 | PASS | `mode:substring` `checkPlayerDepositLimit`, limit 2, 4 pages via returned cursors only. 8 distinct `hitId`s = `candidateCount 8`. Page1 symbols `node_75f18b9a-…`/`node_2d8dcacf-…`; page2 `deposit-limit.service.ts:31` byte 969 and `:43` byte 1329; page3 `deposit-limit.service.ts:69` byte 2113 and `deposit-limit.controller.ts:22` byte 840; page4 `top-up-balance.check.ts:32` byte 1619 and `deposit-limit.service.ts:7` byte 286. Exhaustion deterministic: `truncated:false`, `cursor:null`. **Four separate occurrences inside one file were not collapsed.** Every source occurrence carried filePath+startLine+startByte+endByte+snippet+`contentHash`+`status:"verified"` | The 2 symbol-lane hits carry file+line but no snippet (correctly labelled `kind:"symbol"`, not `source_occurrence`) | none required |
| Q6 Exact / lexical / semantic retrieval triangle | 68 | PARTIAL | Exact: `mode:exact "DepositLimitConverter"` → rank 1, `score 0.95`, `lane:"symbol"`, `nodeId node_0542f63b-0a81-4ffd-bf4d-a2d4979975ca`, evidence `source:"graph"`, `status:"verified"`, 8858.467 ms. Lexical: Natural Intent → `rankReasons ["business intent term coverage=4/5","business intent file coverage=4/5"]`, source lane, verified. Semantic: `mode:"semantic"` → typed `MODE_UNAVAILABLE` / `details.reason "no_active_space"`, `skippedLanes [{lane:"semantic",reason:"no_active_space"}]`, warnings `SEMANTIC_LANE_UNAVAILABLE` ×2, 7248.648 ms | Two defects. (a) `mode:"semantic"` returned `searchedLanes:[]` and zero hits — the deterministic lanes did **not** run as a fallback, while the emitted warning claims "deterministic lanes are partial results". (b) `scope.kinds:["symbol"]` on `VaultFetcher` returned `candidateCount 0` / `NO_MATCH_INCOMPLETE` with `scopeApplied:true`, while the identical query without `kinds` returns it (`node_0a4422f9-…`, `candidateCount 836`) — a **silent false negative** | Fix `scope.kinds` filtering; make `mode:"semantic"` degrade to deterministic lanes or stop emitting a warning that says it did |
| Q7 Semantic generation fixed point | 55 | PARTIAL | `knowledge_semantic_status` read 5 times spanning 07:33:30 → 08:10 (far beyond the 5-second requirement): every response `{"statuses":[]}`. Scoped forms echo resolution: `{requestedScopeKey:"FPMS-NT", resolvedScopeKey:"repo:repo_a48ec7fb-5987-47df-9198-06969359cb50"}` and `{requestedScopeKey:"repo:repo_a48ec7fb-…", resolvedScopeKey:"repo:repo_a48ec7fb-…"}` — also empty. Corroborated by every search: `semantic{ready:0, expected:0, activeGenerationIds:[]}` | No generation exists, so the fixed point is trivially consistent and nothing is falsely `active`. But the capability supplies **none** of the required fields: no expected/ready/pending/running/retryable/terminal counts, no provider/model/revision, no dimensions, no weights/tokenizer/preprocessing digests, no pooling, no normalization, no chunker version, no worker lease/heartbeat/rate/ETA, no canonical scope in the aggregate form. Model identity is only *inferable* from `mcp_health.nativeDependencies` (`nomic-embed-text-v1.5`, `modelHash 4193e4…`) — that is bundle identity, not generation identity. Progress was **not** inferred from elapsed time | Return an explicit zero-state object (counts, provider/model/revision, dimensions, digests, pooling, chunker, worker lease) instead of an empty array |
| Q8 Hybrid evidence decomposition | 45 | BLOCKED | Intended call `mode:auto` + nested `options.semantic:"blend"`, limit 3, FPMS-NT → **`QUERY_TIMEOUT` twice**; `options.semantic:"fallback"` on the same query → **`QUERY_TIMEOUT`**. The identical query with `options.semantic:"off"` completed in 13120.447 ms (`candidateCount 2875`). Substitute single-term hybrid (`checkPlayerDepositLimit`, `semantic:"fallback"`) succeeded and decomposed lanes honestly: `searchedLanes ["source","symbol"]`, `skippedLanes [{lane:"semantic",reason:"no_active_space"}]`, `semantic{requested:true, applied:false, reason:"no_active_space", ready:0, expected:0, activeGenerationIds:[]}`, warning `SEMANTIC_LANE_UNAVAILABLE`; per-hit evidence separated source text (`source:"source"` + contentHash) from graph identity (`source:"graph"` + nodeId) | The scenario as specified could not be executed on a multi-term query. Lane-proof statement therefore rests on the single-term substitute. Vector-similarity evidence was unobtainable (no generation), so nothing was attributed to it | Move semantic orchestration off the deterministic critical path; short-circuit when `activeGenerationIds` is empty |
| Q9 Global context cursor budget | 90 | PASS | `knowledge_context(node_0a4422f9-…, limit:2)`, 3 pages via returned cursors only. Page1 `callers[getEnv pmsServices.ts:229-235, getEnv cpmsServices.ts:102-112]`; page2 `[getEnv cpmsCreditServices.ts:72-79, init clickHouseServices.ts:35-61]`; page3 `[init pcr-clickhouse.service.ts:24-48, fetchKey encryption-codec.ts:111-137]`. **Exactly 2 relation items per page across all relation families combined**, never 2 per family. `orderingKey "relation-offset"`, `lastKey` 2→4→6, zero overlap, `candidateCount 213` stable, same repoId/branchId/snapshotId on all three pages (no drift), cursor continues. Durations 12977.114 / 14492.527 / 13232.337 ms | "No skipped first-hop item within the advertised total" verified only for the first 6 of 213 — exhaustive verification would need 107 pages. `signals` says "10 callers" while `candidateCount` is 213 (different projections, not reconciled) | Publish per-family counts alongside the global `candidateCount` |
| Q10 Cursor support truth table | 62 | PARTIAL | Manifest advertises `supportsCursor:true` for exactly 9 capabilities: `knowledge.search`, `.coverage`, `.context`, `.files`, `.file_symbols`, `.endpoints`, `.dead_code`, `.note.list`, `.saved_query.run`. Verified valid continuation: search (4 pages), files (2 pages), context (3 pages), coverage (2 pages). `dead_code` emitted valid cursors (not continued). `affected` and `flow` honestly do **not** advertise cursor and their input schemas contain no cursor field — no false claim | **`knowledge.endpoints` advertises cursor support, emits a well-formed `nextCursor`, and its continuation dies with `QUERY_TIMEOUT`.** `saved_query.run` untestable: `knowledge_saved_query_list` → `{"items":[]}` (N/A, no saved queries exist). `note.list` and `file_symbols` untested. `knowledge_flow` returns `truncated:true` with `cursor:null`, so its remainder is permanently unreachable | Make the endpoints handler bounded; give `flow` a cursor or stop reporting `truncated:true` |
| Q11 Stable identity round trip | 84 | PASS | Symbol leg: `knowledge_search` → `nodeId node_71666155-b7ab-4ff4-960a-b69ad091aabe`; `get_node(id)` → same nodeId + `identityKey repo_a48ec7fb-…::apps/riskControl/src/antiAddiction/deposit-limit.controller.ts::DepositLimitController`, `kind:"class"`, lines 18–172, `contentHash 11e7b1d3…`, `status:"fresh"`, 125.95 ms; `knowledge_context(nodeId)` → identical identityKey/locator; `knowledge_affected(target:nodeId)` → accepted and echoed `target.requested`; `get_node(identity_key)` → back to the **same** `node_71666155-…` with identical contentHash/lines, 8674.457 ms. Endpoint leg: `knowledge_endpoints(FPMS-Proposal-SDK)` → `node_663234af-…` + `identityKey grpc::proposal.ProposalService.executeproposal` + handler `node_55b8390a-…`; `knowledge_flow` emitted and consumed endpoint identity `node_20863614-…` | The endpoint leg "inventory → continuation page → flow" could not be completed in Largest Repo because `knowledge_endpoints(FPMS-NT)` always times out; the inventory→flow link was proven in a different repo and via `affected.routes`→`flow` in FPMS-NT | Fix endpoints latency so the inventory→continuation→flow chain is exercisable in a large repo |
| Q12 Endpoint continuation & boundary flow | 48 | FAIL (continuation) / PASS (flow) | Continuation attempts: `FPMS-NT limit4 handled_only` → TIMEOUT; `FPMS-NT limit3` → TIMEOUT; `FPMS-NT-Provider limit1 compact protocol:grpc` → **OK at 27894.07 ms** (1 of 17, `nextCursor` emitted, `publication{discovered:17,persisted:17,queryable:17,totalIsExact:true}`); continuation of that very cursor → TIMEOUT; `FPMS-NT-Risk-Control limit2` → TIMEOUT; `FPMS-NT-Recommend limit1` (85 files) → TIMEOUT; `FPMS-Proposal-SDK limit1` → OK at 12520.016 ms, `candidateCount 1`, `truncated:false`, `nextCursor:null` (clean exhaustion). **Three pages were unobtainable anywhere.** Flow: `knowledge_flow(node_2d8dcacf-…, limit 20, compact)` in 10757.926 ms → depth0 endpoint `gRPC riskcontrol.DepositLimitService.CheckPlayerDepositLimit` (`node_20863614-…`) → depth1 handler `checkPlayerDepositLimit` `via:"handles"` (`evidenceState:"proven"`, `method:"EXTRACTED"`, `confidence:1`, provenance `deposit-limit.controller.ts:21`) → depth2 `checkDepositLimit` `via:"calls"` proven at `:25` → depth3 `evaluateState` / `recordDepositLimitChange` / `getRuntimeContext` proven → depth4 **`insertPlayerDepositLimitRecord`** (`deposit-limit-record.service.ts:80-117`, proven at `deposit-limit.service.ts:303`) = persistence boundary. Every hop carried `via` + `graphEvidence`; none was declaration-only | `knowledge_endpoints` is effectively unusable (5 of 7 calls exceeded the 30s hard timeout, including the continuation of its own fresh cursor). Flow reports `truncated:true` with `cursor:null`. The flow root's `filePath` is `apps/payment/.../deposit-limit.service.ts` — a *client* occurrence, not the declaration/proto. `relatedTests` lists `apps/riskControl/test/unit/DepositLimitStatusUtil.test.ts` as a bare file node with **no** edge evidence | Bound the endpoints query; point the endpoint root at its declaration; attach edge evidence to `relatedTests` or rename it `nameAssociatedTests` |
| Q13 Affected parity & test evidence | 78 | PASS with caveats | `knowledge_affected(file:…deposit-limit.controller.ts)` 6112.532 ms and `knowledge_affected(target:node_71666155-…)` 5450.457 ms, same scope/depth/limit, returned **byte-identical** payloads: `changed` 8 symbols, `impacted` 1 (`DepositLimitModule node_5665741f-…`), `dependencies` 12, `tests []`, `routes` 6 (`riskcontrol.DepositLimitService.{CheckPlayerDepositLimit,MockAccumulatedDepositForTest,PlayerDepositLimitState}`, `riskcontrol.FrontendDepositLimitService.{PlayerDepositLimitStateOverview,UpdatePlayerDepositLimitSwitch,UpdatePlayerPeriodDepositLimit}`), `impactEdges []`, `suggestedVerification ["pnpm test","pnpm typecheck"]`, `completeness "lower_bound"`, `proofStatus "candidate"`, `totalIsExact:false`, `candidateCount 9`. Only difference: the symbol form adds `target.requested`. Cross-check: `knowledge_context` on the same node also returned `tests:[]`. **`affected` did not infer coverage from a filename** even though `DepositLimitStatusUtil.test.ts` exists — the honest answer | Symbol granularity collapses to file granularity: naming the class returns all 8 sibling symbols as `changed`. `impactEdges` is empty, so the one `impacted` node has no edge provenance. `knowledge_flow.relatedTests` reintroduces the same test file with no evidence, contradicting `affected.tests:[]` | Honor symbol scope in `affected`; populate `impactEdges`; make `relatedTests` evidence-bearing |
| Q14 Concrete coverage debt & cache hygiene | 91 | PASS | Two non-zero families paged for FPMS-NT. `excluded` (aggregate 7): 5 concrete items with `reasonCode` (`secret_policy` for `.env.example`, `binary`/"NUL byte detected in content sample" for 4 `.ttf`) + `classification`. `unresolved` (aggregate 95718): page1 `external.module.ts:9 "Module" no_enclosing_symbol`, `:11 "registerAsync" no_enclosing_symbol`, `:22 "Error" no_candidate`; page2 (cursor) `:28`/`:29 "getProtoPath" external_package`, `payment-external.service.ts:8 "GetPaymentConfigsReq" unresolved_member_or_type` — **zero overlap**; `remainingCount` 95715 → 95712 exactly tracks 95718−3 and −6; `coverage.reconciliation {status:"reconciled", itemCount:95718, aggregateUnresolved:95718, delta:0}`; `totalIsExact:true`; `gaps ["coverage_debt_requires_remediation"]`. `failed` family: `items:[]`, `candidateCount:0`, `totalIsExact:true`, `truncated:false` — matches aggregate `failed:0`. Cache hygiene, `mode:"path"`, `includeVendor+includeGenerated:true`, explicit 5-repo scope (grpc-web-debugger, claude_code, casino-plus, casino-plus-app, FPMS): `.pnpm-store` 0, `.yarn/` 0, `.npm/` 0, `.bun/install/cache` 0, `node_modules/` 0 **admitted** hits — and `node_modules/` returned `exclusions[{node_modules/.bin/acorn, …anthropic-ai-sdk, …browserslist, …eslint, …glob}]` each `code:"EXCLUDED_FILES_MATCH_PATH", reason:"ignored_by_git"` plus a matching warning. `knowledge_coverage(grpc-web-debugger, excluded)`: 9193 exclusions are `dist/assets/*.js`, `reasonCode:"ignored_by_git"`, `classification:"generated"` | Unscoped search is capped: warning `GLOBAL_SCOPE_CAPPED "unscoped search is limited to 4 repositories; pass an explicit scope for complete coverage"`. The cache negative is therefore asserted only for the 5 explicitly scoped repositories, not all 21 | none required; the cap is correctly disclosed |
| Q15 Read-only owner mutation preflight | 66 | PARTIAL (validation gap) | Schemas inspected without invoking. `knowledge_repository_register` / `knowledge_index` / `knowledge_rebuild` each expose `root_path` (+`path` alias) and `confirmed`, and are `mutating:true`, `confirmation:"required"`, `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`. **Preflight 1** (`register`, `/Users/shieng/Desktop/Projects/fpmsnt`, 2.042 ms): `status:"valid"`, `readOnly:true`, `mutationPerformed:false`, canonical `rootPath`, `confirmationRequired:true`, truthful `annotations`, canonical `nextAction {tool:"knowledge_repository_register", arguments:{root_path, confirmed:true}, ownerApprovalRequired:true}`. **Preflight 2** (`index`, `/System/Library`, 8.123 ms): `ROOT_PATH_OUT_OF_SCOPE` / `"WORKSPACE_SCOPE_DENIED: MCP index root is outside configured workspace roots"` with MCP-native `remediation` preserved. **State unchanged**: `index_status(compact)` before and after are identical (`totalRepos 21, fresh 14, dirty 0, stale 3, unknown 4, errors 0`; every per-repo `indexedCommit`/`headCommit`/`freshness` identical) and `knowledge_semantic_status` still `{"statuses":[]}`. **No permission prompt and no mutation occurred anywhere in this scenario** | **DEFECT**: preflight 3 (`rebuild`, `root_path:"not-an-absolute-path"`) returned `status:"valid"`, silently CWD-joined the value to `/Users/shieng/not-an-absolute-path`, and emitted `nextAction {tool:"knowledge_rebuild", arguments:{root_path:"/Users/shieng/not-an-absolute-path", confirmed:true}}` — a canonical, confirmation-ready action against a non-existent, never-owner-approved root. `MUTATION_PREFLIGHT_INVALID` was **not** returned and schema validation did not block it (the field is a plain `string`). Also, the error path omits `readOnly`/`mutationPerformed`, so read-only proof is absent exactly where a caller most needs it | Reject non-absolute `root_path` with `MUTATION_PREFLIGHT_INVALID`; verify the path exists and is owner-approved before emitting a `confirmed:true` nextAction; include `readOnly`/`mutationPerformed` on error responses |
| Q16 Cursor & root error taxonomy | 74 | PASS with gap | Fresh cursors minted from 5 families: `search`, `files`, `context`, `coverage`, `deadcode`. Valid continuation succeeded for search/files/context/coverage. **Wrong-family** (deadcode cursor → `knowledge_files`): `CURSOR_OPERATION_MISMATCH` "invalid or mismatched files cursor" + `remediation "restart file pagination from the first page using the same repository and revision scope"`, 330.096 ms. **Single-character tamper** (files cursor signature `…BK7o32g` → `…BK7o32h`): `CURSOR_INVALID` + same remediation, 370.99 ms. **The two are distinct typed codes — they do not collapse.** Boundaries: unknown repository → `REPOSITORY_NOT_FOUND` + `remediation "call index_status({\"mode\":\"detailed\"}) and choose an indexed repository"` (3.447 ms) and `SCOPE_NOT_FOUND "repository not found"` from `get_architecture` (79.827 ms); unavailable branch → `SCOPE_NOT_FOUND "branch not found: no-such-branch-round26"` + `details.candidates [{branchName:"brazil-v2", commitSha:"3f0f1984…"}]` + `remediation` (2.312 ms); unknown node → `NODE_NOT_FOUND` + `remediation "call knowledge_search to find a current stable nodeId"` (9642.164 ms); ambiguous target → `TARGET_AMBIGUOUS` + 20 candidates + `remediation` (13318.56 ms) | **Unknown endpoint has no typed boundary error**: `knowledge_flow(target:"grpc::no.such.Service.NoSuchMethodRound26")` burned the full budget and returned `QUERY_TIMEOUT` with **no remediation**; `knowledge_explain` on the same target returned an honest empty (`summary:"target not found"`, `confidence:"unknown"`) instead. `QUERY_TIMEOUT` never carries remediation. Two different codes (`REPOSITORY_NOT_FOUND` vs `SCOPE_NOT_FOUND`) are used for the identical "unknown repo" condition across capabilities | Resolve targets before graph expansion so unknown targets fail fast and typed; attach remediation to `QUERY_TIMEOUT`; unify the unknown-repo code |
| Q17 Compact representation honesty | 86 | PASS | `find_dead_code(FPMS-NT, limit:2)` normal vs `compact:true`, identical scope/limit. Normal: `stats{rawBytesEstimate:3783, sentBytesEstimate:3783, compactRatio:1}`. Compact: `stats{rawBytesEstimate:3783, sentBytesEstimate:2280, compactRatio:0.603}`. **Canonical items identical** (`node_1ab0f1d4-… useFactory external.module.ts:14-40 fileImportedBy 1`, `node_0e1ba2c6-… ExternalModule :48 fileImportedBy 1`); non-zero counts identical (`candidateCount 5526`, `remainingCount 5524`, `totalIsExact:true`); meaning preserved (the false-positive `note` survives verbatim); continuation preserved (valid `nextCursor` in both); caveats preserved (`gaps ["dynamic_dispatch_and_di_not_proven"]`, `proofStatus "candidate"`). Only the duplicated `evidence` mirror disappeared. Other compact ratios observed: endpoints 5760→3584 (0.622) and 6304→4072 (0.646); flow 36446→19676 (0.54). Byte fields are named `rawBytesEstimate`/`sentBytesEstimate` — **self-labelled estimates**, so no impossible self-referential exactness is claimed. Serialization boundary: these are pre-transport JSON sizes of the assembled payload, excluding MCP framing and any transport encoding | Normal-vs-compact could not be compared for `knowledge_endpoints` at identical scope because the non-compact call times out. `knowledge_search` advertises `supportsCompact:true` yet `options.compact:true` produced `compactRatio 1` (no reduction) on every search call | Either implement search compaction or drop `supportsCompact` for `knowledge.search` |
| Q18 Mixed-call latency isolation | 58 | PARTIAL | Interleaved chain, each heavy call measured by its own `timingsMs.total`, `knowledge_semantic_status` between steps (instant, `{"statuses":[]}` every time): search 8858.467 ms (complete page, cursor, no warning beyond `COVERAGE_INCOMPLETE`) → semantic_status → context 7086.912 ms (complete page, cursor) → semantic_status → flow 10757.926 ms (`truncated:true`, no cursor) → affected 6112.532 / 5450.457 ms (complete, no cursor advertised) → service_graph 3669.147 ms (complete, `gaps ["service_graph_is_aggregated"]`) → semantic_status. All within the 30s budget. With semantic off/not-requested, graph and lexical work never waited on embeddings | **Graph/lexical does block on semantic orchestration.** The Q4-family multi-term query completed in 13120.447 ms with `semantic:"off"` but **timed out three times** with `"blend"`(×2) and `"fallback"`(×1). The single-term `"fallback"` call exposes the mechanism: outer `timingsMs.total 12790.265` vs inner `diagnostics.timingsMs.total 2273.823` — **~10.5 s of semantic-orchestration overhead charged outside the measured query even though `activeGenerationIds` is empty**. `get_architecture(FPMS-NT)` took 21054.87 ms. `knowledge_endpoints` and bogus-target `flow` exceeded the budget outright | Skip the semantic path entirely when no generation is active; move it off the request critical path |
| Q19 Scope & negative-proof matrix | 76 | PASS with gaps | Same query across the matrix. Largest Repo: `candidateCount 964`, every hit `repoId repo_a48ec7fb-…`. Second Repo: `candidateCount 480`, every hit `repoId repo_c5cb9c2b-…`. `scopeApplied:true` and `resolvedScopes` echoing branch+commitSha+snapshotId on both — **zero cross-repository and cross-branch leakage**. Unknown repository → `REPOSITORY_NOT_FOUND` + remediation. Unavailable branch → `SCOPE_NOT_FOUND` + candidate branch list. Unknown node → `NODE_NOT_FOUND` + remediation. Wrong cursor family → `CURSOR_OPERATION_MISMATCH`. Tampered cursor → `CURSOR_INVALID`. Negative-proof discipline: **every** empty result carried `queryStatus "NO_MATCH_INCOMPLETE"`, warning `COVERAGE_INCOMPLETE "coverage includes excluded or failed files; an empty result is not proof of absence"`, `NO_MATCH`, and `nextActions [knowledge_coverage({"repo":"<repo>","kind":"failed"})]`; unscoped calls added `GLOBAL_SCOPE_CAPPED`; matched-but-excluded paths surfaced in `exclusions[]`; near-miss spellings surfaced in `suggestions` (`.npmrc`, `npm`, `pnpm`) | Unknown endpoint produced no typed boundary error (`QUERY_TIMEOUT`). `knowledge_explain(target, repo:"FPMS-NT")` returned `scope:null` and corpus-wide coverage (`discovered 33300, admitted 21438, unresolvedReferences 660545`) — **the repo scope was silently ignored**. The `scope.kinds` false negative (Q6) is a product-generated negative that the warning machinery does **not** flag | Honor `repo` in `knowledge_explain`; emit a warning when a scope filter eliminates all candidates |
| Q20 First-user onboarding & Agent handoff | 72 | PASS with defects | `knowledge_onboarding_generate(FPMS-NT)` 18258.475 ms, stamped `revision-hash=0da455e6b903eccd97247013e0aa2293751fed5cebc86fab6d0e83bdd671b6a6 capability-hash=f99fca…`. Examples use correct **nested** shapes: `knowledge_search({query, mode, scope:{revisions:[{repoName,branch}]}, options:{semantic:"off",compact:true}, page:{limit:20}})`, cursor guidance ("下一页沿用同一 query/scope/options，只把 page 改为 `{page:{limit:20,cursor:"<nextCursor>"}}`"), `index_status({"mode":"detailed"})`, `knowledge_coverage`, `knowledge_semantic_status({"scopeKey":"repo:repo_a48ec7fb-…"})` with the correct stable scopeKey form and the correct instruction to fall back to graph/lexical when semantic is unavailable, `knowledge_explore`, `knowledge_affected`, plus an explicit rule that negative/partial/lower_bound/stale results require checking `coverage`/`freshness`/`proofStatus`. Owner-only CLI is segregated in §10. **Handoff**: Agent A package contained only MCP-emitted state — `{repoName:"FPMS-NT", branch:"brazil-v2", snapshotId:"snapshot_c7bf7942-…", commitSha:"3f0f1984…", nodeId:"node_0542f63b-…", identityKey:"repo_a48ec7fb-…::…/deposit-limit-converter.ts::DepositLimitConverter", unusedCursor:"eyJ…cNSBaUqOz50gMUUcRi8jZR1TIglhsTyeZ8oCq1QLEPU", caveats:[COVERAGE_INCOMPLETE, semantic no_active_space, totalIsExact:false]}`. Agent B resumed from the cursor alone: returned the next distinct hit `hit_ffb9e6f19db4c4c8b17b6f59` (`deposit-limit-converter.ts:5`, bytes 233–254, contentHash `5c727d9a…`, verified), same `snapshotId`/`commitSha`, `candidateCount 17` unchanged, **no rediscovery and no drift** | Two onboarding examples name capability ids that are **not** MCP tool ids: `knowledge_get_node({"id":…})` (actual tool: `get_node`) and `knowledge_architecture({"repo":…})` (actual tool: `get_architecture`). A first-time MCP-only consumer copying them gets tool-not-found. Onboarding states "当前 revision endpoints：20" while `get_architecture` reports `nodeCounts.endpoint 608` — no reconciliation between the two, and `knowledge_endpoints(FPMS-NT)` cannot be paged to settle it | Emit MCP tool ids in onboarding examples; reconcile canonical-inventory vs endpoint-node counts |

## 4. B1–B8 bonus scenario scores

Bonus scenarios add no points. Where they expose a defect they reduce the named category.

| Scenario | Score /100 | Verdict | Direct MCP evidence | Limitation/failure | Remediation |
| --- | ---: | --- | --- | --- | --- |
| B1 Cross-service relation proof | 88 | PROVEN (calls), bounded | `knowledge_service_graph(FPMS-NT)`: FPMS-NT → FPMS-NT-Payment `edgeType:"invokes"`, `evidenceState:"proven"`, `method:"EXTRACTED"`, `confidence:1`, `scope:"environment"`, provenance `apps/admin/inteceptor/payment-external.service.ts:32`; reverse FPMS-NT-Payment → FPMS-NT `invokes`, provenance `libs/tools/src/client-grpc/admin-client-grpc.ts:54`. `depends_on` edges exist in the graph but only toward FPMS-NT-Shared (provenance `~package.json`, `scope:"revision"`). Conclusion: the pair **mutually invokes** over gRPC clients; it does not `own` either side; `depends_on` is a separate package-level relation not present between this pair; `references` is subsumed by the stronger proven `invokes` | Uncertainty preserved by the product: `gaps ["service_graph_is_aggregated"]`, `completeness "lower_bound"`, `proofStatus "not_proven"`, `candidateCount 84`. Provenance is a client-stub *construction site* — it proves a client exists, not that a call executes at runtime. All 84 edges carry the focus repo's `revisionId` even for edges between other repos | Stamp each aggregated edge with its own source revision |
| B2 Minimal safe-change envelope for Primary File | 84 | USABLE | **Proven blast radius**: 8 symbols redefined in-file; 1 impacted (`DepositLimitModule node_5665741f-…`); 12 dependencies incl. `checkDepositLimit`, `toStateProto`, `toConfigProto`, `DepositLimitService`, 2 DTOs, `TokenGetPlayerIdGuardRequest`; 6 gRPC routes; flow proves the write path to `insertPlayerDepositLimitRecord`. **Minimal plan**: (1) change only the 6 handler bodies, never the `@GrpcMethod` service/method strings — those are the 6 published route identities; (2) any signature change to `DepositLimitConverter.toStateProto/toConfigProto` propagates to all 6 routes (verified via flow depth 2–3); (3) `DepositLimitModule` is the only proven consumer to re-check; (4) persistence contract changes must stop at `deposit-limit-record.service.ts:80-117` | **Lower-bound risk**, explicitly declared by the product: `completeness "lower_bound"`, `proofStatus "candidate"`, `totalIsExact:false`, and the note "constructor calls, interface dispatch, static-method calls and calls inside callback bodies are not modelled". Repo carries 95718 unresolved references. `impactEdges:[]` — the one impacted node is unexplained. `tests:[]` — **no proven executable coverage**; `DepositLimitStatusUtil.test.ts` appears only as `flow.relatedTests` with no edge, so it must not be treated as coverage. `suggestedVerification ["pnpm test","pnpm typecheck"]` is manifest-derived, not proven. Revision caveat: aligned at `3f0f1984…`, worktree clean, but `indexedSchemaVersion 17 < 18` | Populate `impactEdges`; make test association evidence-bearing |
| B3 Retrieval disagreement | 52 | NOT EXECUTABLE as specified | No embedding generation exists (`statuses:[]`, `activeGenerationIds:[]`), so vector ranking never produced a competing order and no exact/graph-vs-semantic disagreement could be exhibited. Two **substitute** disagreements were found and resolved in favour of direct evidence: (a) `get_architecture.hubs` lists `PromotionRedisService` as a *symbol* with degree 91, yet `knowledge_context("PromotionRedisService")` → `TARGET_AMBIGUOUS` whose 20 candidates are **all `nodeType:"field"`** named `promotionRedisService` and never the class — name-based discovery contradicts hub reporting; (b) `scope.kinds:["symbol"]` returns `candidateCount 0` for `VaultFetcher` while the unfiltered query returns `node_0a4422f9-…` with verified graph evidence. In both cases the factual claim follows the direct evidence (`get_node`/graph nodeId), not the ranking surface | Primary scenario unexecutable; reduces category 4. The ambiguity payload is also weakly actionable: all 20 candidates have `filePath:null`, `branch:null`, `startLine:null` | Include filePath/branch/startLine in `TARGET_AMBIGUOUS` candidates; make class symbols reachable by name |
| B4 Historical generation hygiene | 50 | UNPROVEN | `knowledge_semantic_status` returned `{"statuses":[]}` on 5 reads spanning ~37 minutes, in aggregate and scoped forms. No generation is reported as active, staging, or superseded — therefore nothing is simultaneously active and nothing is permanently counted as current work | Vacuous pass. The capability exposes no generation history whatsoever, so supersession behaviour cannot be verified in either direction. This is not evidence of correct hygiene | Retain and expose superseded generations with explicit lifecycle state |
| B5 Semantic control discoverability | 90 | PASS (no invocation) | `knowledge_semantic_control` input schema read without calling it: `action` enum `[pause, resume, retry, cancel]`; `generationId` "Required for retry and cancel; forbidden for pause and resume"; `scopeKey` required ("Required repo scope key"); `operationToken` required, "Required caller-generated correlation/idempotency token", `minLength 8`; `confirmation_token` required, "MCP transport authorization confirmation; separate from the canonical operationToken"; `additionalProperties:false`. Manifest: `mutating:true`, `confirmation:"required"`, `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false`. **Valid in the current state: none.** `retry`/`cancel` require a `generationId` and no generation exists (`activeGenerationIds:[]`); `pause`/`resume` have no queued or running work to act on (`statuses:[]`) | Discoverability is complete from the schema alone; no state-machine document is exposed to say which action is legal in which state — that had to be derived | Publish per-action preconditions in the capability manifest |
| B6 Fresh MCP worker wake contract | 50 | UNPROVEN / BLOCKED BY BOUNDARY | In this fresh MCP process `knowledge_semantic_status` is empty at 07:33:30 and still empty at 08:10 — **no queued semantic work existed**, so no wake could be observed and none is claimed. The only wake path is `knowledge_semantic_control`, which is `mutating:true` + `confirmation:"required"` and is forbidden by the evaluator boundary. Indirect signal: a search with `options.semantic` requested charges ~10.5 s of orchestration outside the measured query (outer 12790.265 ms vs inner 2273.823 ms) even with zero generations, which suggests a provider runtime is contacted per query rather than short-circuited | Cannot conclude either way. Explicitly **not** claiming a wake occurred | Expose a read-only worker/lease/heartbeat projection in `semantic_status` so the wake contract is observable without mutation |
| B7 Remediation usability | 80 | PASS with gap | Remediation collected from 9 partial/negative results, all actionable by an MCP-only consumer with no source/DB/CLI: `NODE_NOT_FOUND` → "call knowledge_search to find a current stable nodeId"; `SCOPE_NOT_FOUND`(repo) → "specify a registered repository"; `SCOPE_NOT_FOUND`(branch) → "specify branch, commit, or snapshot" **plus** `candidates[{branchName, commitSha}]`; `REPOSITORY_NOT_FOUND` → the literal next call `index_status({"mode":"detailed"})`; `TARGET_AMBIGUOUS` → "call knowledge_search with repo and branch fields, then retry with an exact nodeId from candidates" + 20 candidates; `CURSOR_OPERATION_MISMATCH` and `CURSOR_INVALID` → "restart file pagination from the first page using the same repository and revision scope"; `ROOT_PATH_OUT_OF_SCOPE` → "choose a repository inside the configured workspace roots or update the owner-controlled MCP workspace configuration"; every `NO_MATCH` → `nextActions [knowledge_coverage({"repo":"<repo>","kind":"failed"})]` with the reason "only the owner may refresh it with knowledge_index"; `.npm/` miss → `suggestions [.npmrc, npm, pnpm]` | **`QUERY_TIMEOUT` — the single most frequent failure in this evaluation (9 occurrences) — carries no remediation and no hint about which parameter to shrink.** `ROOT_PATH_OUT_OF_SCOPE` drops `readOnly`/`mutationPerformed`. `TARGET_AMBIGUOUS` candidates carry null filePath/branch/startLine | Attach remediation to `QUERY_TIMEOUT` naming the parameter to reduce (limit, scope, protocol/path filter) |
| B8 Readiness verdict by lane | 85 | DIAGNOSTIC | **Exact/graph investigation — READY.** Exact/phrase/substring/path/symbol lanes give verified file:line:byte:snippet + contentHash, correct multi-page continuation, deterministic exhaustion, and `flow` reaches a persistence boundary with per-hop proven evidence. **Semantic discovery — NOT READY.** Zero generations; hybrid orchestration breaks the latency budget. **Runtime reliability — NOT READY.** 9 hard timeouts; `knowledge_endpoints` unusable; 21 s architecture; 1.9× variance on identical input. **File/coverage completeness — READY.** Reconciliation equations satisfied, coverage debt concrete/pageable/bounded, cache hygiene clean with explicit exclusion metadata. **Identity/continuation stability — READY with one hole.** Identity round trip is exact in both directions and wrong-family vs tampered cursors are distinctly typed; the hole is endpoints' undeliverable advertised cursor. **Owner-only Tauri UX — N/A_OWNER_EVIDENCE_NOT_SUPPLIED.** **Strongest safe use**: "given a symbol or identifier in an aligned repo, prove where it is defined, which occurrences exist with byte-exact provenance, what its first-hop relations are, what a change to its file provably touches, and trace its endpoint to the persistence call." **Strongest unsafe claim**: "X does not exist / X is not used / X is not covered by tests" — every empty result is `NO_MATCH_INCOMPLETE` against 95718 unresolved references, 7 excluded files, 35 source-only files, a `scope.kinds` filter that silently zeroes valid hits, an endpoints inventory that cannot be enumerated, and `tests:[]` meaning "no modelled edge", not "no test" | — | — |

## 5. Required evidence tables

### 5.1 Repository / file measures (Largest Repo = FPMS-NT)

| Repository/file measure | Aggregate | Page evidence | Reconciliation equation or bound | Safe conclusion |
| --- | ---: | --- | --- | --- |
| Admitted source files | 3333 | `candidateCount 3333`, `totalIsExact:true` at limits 5/2/4 | `admittedSourceFiles = sourceAndGraphFiles + sourceOnlyFiles` → 3298+35=3333 ✓ | Exact |
| Graph-parsed files | 3298 | `reconciliation.graphParsedFiles` | `graphParsedFiles = sourceAndGraphFiles + graphOnlyFiles` → 3298+0=3298 ✓ | Exact |
| Source-and-graph files | 3298 | — | — | Exact |
| Source-only files | 35 | samples `.dockerignore`, `.github/actions/setup-node/action.yml`, `.github/codex/prompts/review.md` | 3333−3298=35 ✓ | Exact. Admitted-and-searchable need not contain graph symbols: these are non-parsed formats (dockerignore, YAML, Markdown) admitted for text/path search but producing no symbol nodes |
| Graph-only files | 0 | — | `candidateFiles = admittedSourceFiles + graphOnlyFiles` → 3333+0=3333 ✓ | Exact |
| Candidate/global total | 3333 | `candidateCount` | — | Exact |
| Page item count / returnedCount | 5, 2, 4 | `returnedCount` matched `items.length` on every page | `remainingCount = 3333 − returnedCount` → 3328/3331/3329 ✓ | Exact |
| Discovered / excluded / failed / stale | 3340 / 7 / 0 / 0 | `knowledge_coverage` returned 5 concrete excluded items with reasonCode+classification; `failed` family empty and `totalIsExact:true` | 3333+7=3340 ✓ | Exact |
| Unresolved references | 95718 | 6 concrete items across 2 pages, 4 distinct reason codes | `reconciliation {itemCount 95718, aggregateUnresolved 95718, delta 0}` | Exact count, `completeness "lower_bound"` for interpretation |
| Architecture file nodes | 3306 | `get_architecture.nodeCounts.file` | **No equation supplied**; 3306−3298=8 unexplained | **Unreconciled** — recorded as a defect |
| Endpoint count | 608 vs 20 | `get_architecture.nodeCounts.endpoint 608`; onboarding "当前 revision endpoints：20"; `knowledge_flow candidateCount 20` | **No equation supplied** | **Unreconciled**; `knowledge_endpoints(FPMS-NT)` times out so it cannot be settled |
| Second Repo (casino-plus) | admitted 2222 / excluded 130 / discovered 2352 | search returned only `repo_c5cb9c2b-…` hits | 2222+130=2352 ✓ | Exact |

### 5.2 Query / retrieval lanes

| Query/retrieval lane | Repo | Duration (ms) | Candidate/root/evidence | Requested/applied/generation | Top identities | Warning |
| --- | --- | ---: | --- | --- | --- | --- |
| Natural Intent, auto (Q4 A1) | FPMS-NT | 4919.974 | 964 / n-a / 8 verified source occurrences | false / false / `not_requested`, `activeGenerationIds:[]` | `errorCodeConstants.ts:46`; `node_71666155-…` DepositLimitController | COVERAGE_INCOMPLETE |
| Natural Intent, auto (Q4 A2) | FPMS-NT | 2497.785 | 962 / n-a / 3 | false / false / `not_requested` | identical hitIds, identical scores | COVERAGE_INCOMPLETE |
| Natural Intent, auto (Q4 A2 repeat) | FPMS-NT | 4792.35 | 962 / n-a / 3 | false / false / `not_requested` | identical (deterministic) | COVERAGE_INCOMPLETE |
| Natural Intent, auto (Q4 B1) | casino-plus | 4223.838 | 480 / n-a / 3 | false / false / `not_requested` | `node_73082685-…`; `node_cc735f2b-…` | COVERAGE_INCOMPLETE |
| Natural Intent, auto (Q4 B2) | casino-plus | 3254.959 | 480 / n-a / 3 | false / false / `not_requested` | identical (deterministic) | COVERAGE_INCOMPLETE |
| Exact symbol `checkPlayerDepositLimit` | FPMS-NT | 5762.709 | 8 / n-a / 2 graph-verified symbols | false / false / `not_requested` | `node_75f18b9a-…`, `node_2d8dcacf-…` | COVERAGE_INCOMPLETE |
| Substring, 4 pages | FPMS-NT | 5050.136 / 4205.423 / 4763.629 / 4619.468 | 8 / n-a / 8 distinct occurrences | false / false / `not_requested` | 4 occurrences in `deposit-limit.service.ts` lines 7/31/43/69 | COVERAGE_INCOMPLETE |
| Path lane `node_modules/` | 5-repo scope | 3258.405 | 0 / n-a / `exclusions[5]` | false / false / `not_requested` | none admitted | COVERAGE_INCOMPLETE, NO_MATCH, **EXCLUDED_FILES_MATCH_PATH** |
| `mode:"semantic"` paraphrase | FPMS-NT | 7248.648 | 0 / n-a / none | **true / false / `no_active_space`** | none | SEMANTIC_LANE_UNAVAILABLE ×2, NO_MATCH; error `MODE_UNAVAILABLE` |
| 9-word query, `semantic:"off"` | FPMS-NT | 13120.447 | 2875 / n-a / 3 verified | false / false / `not_requested` | `errorCodeConstants.ts:48` | COVERAGE_INCOMPLETE |
| 9-word query, `semantic:"blend"` ×2 | FPMS-NT | **>30000 (TIMEOUT)** | — | true / — / — | — | `QUERY_TIMEOUT`, no remediation |
| 9-word query, `semantic:"fallback"` | FPMS-NT | **>30000 (TIMEOUT)** | — | true / — / — | — | `QUERY_TIMEOUT`, no remediation |
| 1-term, `semantic:"fallback"` | FPMS-NT | 12790.265 outer / 2273.823 inner | 7 / n-a / 1 | true / false / `no_active_space` | `node_75f18b9a-…` | SEMANTIC_LANE_UNAVAILABLE |
| `scope.kinds:["symbol"]` VaultFetcher | FPMS-NT | 3992.574 | **0** / n-a / none | false / false / `not_requested` | **none — false negative** | COVERAGE_INCOMPLETE, NO_MATCH (no filter warning) |
| No-kinds VaultFetcher | FPMS-NT | 3968.053 | 836 / n-a / 1 verified | false / false / `not_requested` | `node_0a4422f9-…` `vault-fetcher.ts:8` | COVERAGE_INCOMPLETE |
| `knowledge_flow` Primary Endpoint | FPMS-NT | 10757.926 | 20 / root `node_20863614-…` / all hops `evidenceState:"proven"` | n-a | depth4 `insertPlayerDepositLimitRecord` | none; `proofStatus "not_proven"` at collection level |
| `knowledge_context` Relation-rich ×3 | FPMS-NT | 12977.114 / 14492.527 / 13232.337 | 213 / `node_0a4422f9-…` / 6 callers with file:line | n-a | `getEnv`×3, `init`×2, `fetchKey` | none |
| `knowledge_affected` file / symbol | FPMS-NT | 6112.532 / 5450.457 | 9 / — / 8 changed + 1 impacted + 12 deps + 6 routes | n-a | `node_5665741f-…` DepositLimitModule | none; `completeness "lower_bound"` |
| `get_architecture` | FPMS-NT | **21054.87** | 133,283 nodes / — / hubs+entryPoints | n-a | `VaultFetcher` degree 297 | none |
| `knowledge_endpoints` | FPMS-NT ×2, Risk-Control, Recommend, Provider-continuation | **>30000 (TIMEOUT) ×5** | — | n-a | — | `QUERY_TIMEOUT`, no remediation |
| `knowledge_endpoints` | FPMS-NT-Provider | 27894.07 | 17 / — / 1 with occurrence provenance | n-a | `node_acc31eb7-…` `handlerStatus:"incomplete"`, `missingHandlerReason:"no_active_handles_edge"` | none |
| `knowledge_endpoints` | FPMS-Proposal-SDK | 12520.016 | 1 / — / handler proven | n-a | `node_663234af-…`, handler `node_55b8390a-…` | none |

### 5.3 Cursor families

| Cursor family | Advertised | Schema accepts | Valid continuation | Overlap/drift | Wrong-family | Tampered | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `search` | yes | yes (`page.cursor`) | yes — 4 pages to clean exhaustion (`truncated:false`, `cursor:null`) | none; 8 distinct hitIds | not tested from this side | not tested from this side | PASS |
| `files` | yes | yes (`cursor`) | yes — 2 pages, `lastKey` advanced `.dockerignore` → `.github/actions/setup-node/action.yml` | none | `CURSOR_OPERATION_MISMATCH` + remediation | `CURSOR_INVALID` + remediation | PASS |
| `context` | yes | yes (`cursor`) | yes — 3 pages, `lastKey` 2→4→6 | none; global limit 2 honored; same snapshot on all pages | not tested | not tested | PASS |
| `coverage` | yes | yes (`cursor`) | yes — 2 pages, `remainingCount` 95715→95712 | none | not tested | not tested | PASS |
| `dead_code` | yes | yes (`cursor`) | cursor emitted and well-formed; continuation not exercised | n-a | n-a | n-a | PARTIAL |
| `endpoints` | **yes** | yes (`cursor`) | **NO — continuation of its own fresh cursor returned `QUERY_TIMEOUT`** | n-a | n-a | n-a | **FAIL** |
| `saved_query.run` | yes | yes (`cursor`) | **N/A** — `knowledge_saved_query_list` → `{"items":[]}`, no saved query exists to run | n-a | n-a | n-a | N/A |
| `file_symbols` | yes | yes (`cursor`) | not tested | n-a | n-a | n-a | UNTESTED |
| `note.list` | yes | yes | not tested (note surface out of investigation path) | n-a | n-a | n-a | UNTESTED |
| `affected` | **no** | no cursor field in schema | n-a — honestly unsupported | n-a | n-a | n-a | PASS (honest) |
| `flow` | **no** | no cursor field in schema | n-a — but returns `truncated:true` with `cursor:null`, so the remainder is unreachable | n-a | n-a | n-a | PARTIAL (honest but lossy) |

### 5.4 Preflight and error cases

| Preflight/error case | Read-only proof | Typed code | Preserved remediation | State unchanged | Verdict |
| --- | --- | --- | --- | --- | --- |
| Preflight `register` on owner root `/Users/shieng/Desktop/Projects/fpmsnt` | `readOnly:true`, `mutationPerformed:false` | `status:"valid"` | canonical `nextAction {knowledge_repository_register, confirmed:true, ownerApprovalRequired:true}` | yes — `index_status` identical before/after | PASS |
| Preflight `index` on out-of-scope `/System/Library` | implied (error path, no mutation) — fields **absent** | `ROOT_PATH_OUT_OF_SCOPE` / `WORKSPACE_SCOPE_DENIED` | yes, MCP-native | yes | PASS with gap |
| Preflight `rebuild` on malformed `root_path:"not-an-absolute-path"` | `readOnly:true`, `mutationPerformed:false` | **`status:"valid"` — `MUTATION_PREFLIGHT_INVALID` NOT returned**; silently CWD-joined to `/Users/shieng/not-an-absolute-path` and emitted a `confirmed:true` nextAction | n-a (no error raised) | yes | **FAIL** |
| Unknown repository (`knowledge_search` scope) | n-a | `REPOSITORY_NOT_FOUND` | `index_status({"mode":"detailed"})` | yes | PASS |
| Unknown repository (`get_architecture`) | n-a | `SCOPE_NOT_FOUND` | "specify a registered repository" | yes | PASS (code inconsistent with the above) |
| Unavailable branch (`knowledge_files`) | n-a | `SCOPE_NOT_FOUND "branch not found"` | "specify branch, commit, or snapshot" + `candidates[{brazil-v2, 3f0f1984…}]` | yes | PASS |
| Unknown node (`get_node`) | n-a | `NODE_NOT_FOUND` | "call knowledge_search to find a current stable nodeId" | yes | PASS |
| Ambiguous target (`knowledge_context`) | n-a | `TARGET_AMBIGUOUS` | "call knowledge_search with repo and branch fields, then retry with an exact nodeId" + 20 candidates | yes | PASS (candidates lack filePath/branch/line) |
| Wrong-family cursor (deadcode → files) | n-a | `CURSOR_OPERATION_MISMATCH` | "restart file pagination from the first page…" | yes | PASS |
| Tampered cursor (1 char) | n-a | `CURSOR_INVALID` | "restart file pagination from the first page…" | yes | PASS — distinct from wrong-family |
| Unknown endpoint (`knowledge_flow`) | n-a | **`QUERY_TIMEOUT`** — no typed target error | **none** | yes | **FAIL** |
| Semantic with no generation (`mode:"semantic"`) | n-a | `MODE_UNAVAILABLE` / `reason "no_active_space"` | warnings only, no nextAction for the owner | yes | PASS with gap |
| Semantic blend/fallback on multi-term query | n-a | **`QUERY_TIMEOUT`** ×3 | **none** | yes | **FAIL** |
| `scope.kinds:["symbol"]` filter | n-a | **none — `queryStatus "NO_MATCH_INCOMPLETE"`, `scopeApplied:true`, no filter warning** | generic coverage nextAction only | yes | **FAIL (silent false negative)** |

### 5.5 Coverage families

| Coverage family | Aggregate | Concrete page evidence | Exact/lower bound | Cursor | Safe conclusion |
| --- | ---: | --- | --- | --- | --- |
| `excluded` (FPMS-NT) | 7 | `.env.example` `secret_policy`/`secret`; 4× `Roboto-*.ttf` `binary`/"NUL byte detected in content sample" | `totalIsExact:true`, `completeness "lower_bound"` | emitted | Exact count; exclusions are policy/binary, not source loss |
| `unresolved` (FPMS-NT) | 95718 | p1: `external.module.ts:9 "Module" no_enclosing_symbol`, `:11 "registerAsync" no_enclosing_symbol`, `:22 "Error" no_candidate`; p2: `:28`,`:29 "getProtoPath" external_package`, `payment-external.service.ts:8 "GetPaymentConfigsReq" unresolved_member_or_type` | `totalIsExact:true`; `reconciliation delta 0`; `gaps ["coverage_debt_requires_remediation"]` | emitted, continued, no overlap, `remainingCount` 95715→95712 | Exact count, bounded and pageable; large unresolved debt means graph negatives are lower bounds |
| `failed` (FPMS-NT) | 0 | `items:[]` | `totalIsExact:true`, `truncated:false` | `null` | Genuinely zero; matches aggregate |
| `stale` (FPMS-NT) | 0 | aggregate only | `totalIsExact:true` | n-a | Zero |
| `excluded` (grpc-web-debugger) | 9193 | 4× `dist/assets/*.js` `ignored_by_git`/`generated` | `totalIsExact:true`, `remainingCount 9189` | emitted | Exclusions are git-ignored build output, not package cache; `admitted 45` |
| Cache pollution (5-repo scope) | 0 admitted | `.pnpm-store` 0, `.yarn/` 0, `.npm/` 0, `.bun/install/cache` 0, `node_modules/` 0 admitted + `exclusions[5]` `ignored_by_git` | n-a | n-a | **Not admitted/indexed in current evidence for the 5 explicitly scoped repositories.** No claim made for the other 16 (unscoped search is capped at 4 repos per `GLOBAL_SCOPE_CAPPED`) |

### 5.6 Mixed-call latency (Q18 chain, 30s hard budget)

| Mixed call | Duration (ms) | Complete/truncated | Cursor/warning | 30-second budget |
| --- | ---: | --- | --- | --- |
| `knowledge_semantic_status` | <100 | complete (`statuses:[]`) | none | PASS |
| `knowledge_search` exact limit 1 | 8858.467 | page complete, `truncated:true` | `nextCursor` / COVERAGE_INCOMPLETE | PASS |
| `knowledge_semantic_status` | <100 | complete | none | PASS |
| `knowledge_context` limit 1 | 7086.912 | page complete, `truncated:true` | `cursor` / none | PASS |
| `knowledge_semantic_status` | <100 | complete | none | PASS |
| `knowledge_flow` limit 20 compact | 10757.926 | **`truncated:true`** | **no cursor** / none | PASS |
| `knowledge_affected` file | 6112.532 | complete, `truncated:false` | none / none | PASS |
| `knowledge_affected` symbol | 5450.457 | complete, `truncated:false` | none / none | PASS |
| `knowledge_service_graph` | 3669.147 | complete, `truncated:false` | none / `gaps ["service_graph_is_aggregated"]` | PASS |
| `knowledge_semantic_status` | <100 | complete | none | PASS |
| Q4 multi-term with `semantic:"off"` | 13120.447 | page complete | `nextCursor` | PASS |
| Q4 multi-term with `semantic:"blend"` ×2 | **>30000** | **failed** | `QUERY_TIMEOUT`, no remediation | **FAIL** |
| Q4 multi-term with `semantic:"fallback"` | **>30000** | **failed** | `QUERY_TIMEOUT`, no remediation | **FAIL** |
| `get_architecture` | 21054.87 | complete | none | PASS (marginal) |
| `knowledge_endpoints` (5 calls) | **>30000 each** | **failed** | `QUERY_TIMEOUT`, no remediation | **FAIL** |
| `knowledge_flow` bogus target | **>30000** | **failed** | `QUERY_TIMEOUT`, no remediation | **FAIL** |

### 5.7 Owner-only Tauri evidence — not MCP-scored

| Item | Status |
| --- | --- |
| Wiki opens on Graph with Focus absent | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` |
| Semantic state / progress / model visible | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` |
| Pause/Resume/Retry/Cancel appear only when valid | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` |
| Background work survives App closure | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` |
| Graph/Storage stay responsive | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` |
| Installed build identity matches MCP | `N/A_OWNER_EVIDENCE_NOT_SUPPLIED` (MCP side reports `1.16.0-f5de64b02a83a582`, running == available) |

No owner screenshots or observations were supplied. This section does not change the MCP score.

## 6. Hard gates

| Hard gate | PASS/FAIL | Direct evidence | Score impact |
| --- | --- | --- | --- |
| 1. MCP unavailable or unloaded in the fresh OS process | PASS | `mcp_health status:"ok"`, `configured:true`, `launcherHealthy:true`, `initializeHealthy:true`, session `5703228d-…` | none |
| 2. Running/available build mismatch, restart required, or schema/contract/hash conflict | **FAIL** | Build parity is clean (`runningBuildId == availableBuildId`, `restartRequired:false`) and contract/capability/model hashes are consistent on every response. **But** `trust.indexedSchemaVersion 17` ≠ `trust.schemaVersion 18` on every context/flow response: the persisted index was built under a different schema than the runtime. Disclosed, never reconciled, no remediation, no restart/reindex signal | Caps final <95; −category 1 |
| 3. Semantic use claimed without a compatible complete active generation and full model identity | PASS | Semantic is never claimed applied: `semantic{requested,applied:false,reason}` on every call; `MODE_UNAVAILABLE`/`no_active_space` when explicitly requested; `activeGenerationIds:[]` throughout | none |
| 4. Any of the four Q4 calls exceeds the 30-second hard budget | PASS | 4919.974 / 2497.785 / 4792.35 / 4223.838 / 3254.959 ms — max 4.92 s | none |
| 5. Exact occurrence pages collapse distinct occurrences or overlap/drift | PASS | 4 pages, 8 distinct hitIds = `candidateCount 8`; 4 occurrences in one file at distinct startBytes 286/969/1329/2113 all returned separately; deterministic exhaustion | none |
| 6. Context pages exceed the global limit, repeat, skip, drift, or cannot continue | PASS | 3 pages × exactly 2 relation items across all families; `lastKey` 2→4→6; no overlap; identical snapshotId on all pages | none |
| 7. Advertised cursor support ignores input or cannot produce valid continuation | **FAIL** | `knowledge.endpoints` manifest `supportsCursor:true`; page 1 on FPMS-NT-Provider emitted a well-formed `nextCursor`; passing that cursor back returned `QUERY_TIMEOUT`. No endpoints continuation succeeded in any repo | Caps final <95; −category 5 |
| 8. A freshly emitted node/endpoint identity fails round trip | PASS | `node_71666155-…` and its identityKey survived search → get_node(id) → context → affected → get_node(identity_key) with identical path/kind/lines/contentHash; endpoint identity `node_663234af-…`/`node_20863614-…` accepted by flow | none |
| 9. Admitted-source, graph-parsed, and source-only counts do not reconcile honestly | PASS (caveat) | `reconciliation` block publishes three equations and all three hold (3298+35=3333; 3298+0=3298; 3333+0=3333), `reconciles:true`, `totalIsExact:true`. Caveat recorded separately: `architecture.nodeCounts.file 3306` and the 608-vs-20 endpoint counts are not reconciled to anything | −category 6 (caveat only) |
| 10. Concrete non-zero coverage debt cannot be paged or bounded | PASS | `excluded` 7 and `unresolved` 95718 both returned concrete items with reasonCodes, paged with cursors, exact `remainingCount` arithmetic, `reconciliation delta 0` | none |
| 11. Package-cache files pollute current source evidence | PASS | All five named cache patterns + `node_modules/` returned zero admitted hits under explicit 5-repo scope; matches surfaced only as `exclusions[]` with `EXCLUDED_FILES_MATCH_PATH` / `ignored_by_git` | none |
| 12. Mutation discovery requires calling a mutating tool, triggers permission, or changes state | PASS | All discovery via `knowledge_capabilities` schemas + `knowledge_doctor.mutation_preflight`. No mutating tool invoked, no permission prompt, `index_status` and `semantic_status` identical before/after | none |
| 13. Mutation schemas/annotations omit root, confirmation, or truthful safety hints | PASS | register/index/rebuild each expose `root_path` (+`path`) and `confirmed`; manifest and preflight both report `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `confirmation:"required"` | none |
| 14. Wrong-family and tampered cursors collapse into the same misleading result | PASS | `CURSOR_OPERATION_MISMATCH` vs `CURSOR_INVALID` — distinct typed codes, both with remediation | none |
| 15. Typed root/cursor errors lose actionable `remediation` | PASS (caveat) | Every root and cursor error preserved `remediation`, including nested and top-level copies. Caveat: `QUERY_TIMEOUT` — neither a root nor a cursor error — carries none, and it is the most common failure here | −category 2 (caveat only) |
| 16. Graph/Lexical blocks on semantic work | **FAIL** | Identical multi-term query: `semantic:"off"` → 13120.447 ms MATCH; `semantic:"blend"` → `QUERY_TIMEOUT` (×2); `semantic:"fallback"` → `QUERY_TIMEOUT`. Single-term probe quantifies it: outer 12790.265 ms vs inner `diagnostics.timingsMs.total 2273.823` = ~10.5 s of semantic orchestration charged with `activeGenerationIds:[]` | Caps final <95; −categories 3 and 4 |
| 17. Compact output changes meaning/counts or presents impossible byte claims as exact | PASS | dead_code normal vs compact at identical scope: identical items, `candidateCount 5526`, `totalIsExact:true`, note, `gaps`, cursor; only the duplicated `evidence` mirror removed; 3783→2280 (0.603). Byte fields named `rawBytesEstimate`/`sentBytesEstimate` — estimates, not claimed exact | none |
| 18. Negative conclusions ignore exclusion/failure/staleness/unresolved/truncation/source-only/revision limits | **FAIL** | The product annotates negatives well (`NO_MATCH_INCOMPLETE`, `COVERAGE_INCOMPLETE`, `GLOBAL_SCOPE_CAPPED`, `exclusions[]`, `suggestions`, coverage `nextActions`). **But** `scope.kinds:["symbol"]` produced `candidateCount 0` for a symbol the same query finds unfiltered, reporting `scopeApplied:true` with **no filter warning** — a product-generated false negative that the negative-proof machinery does not catch. `knowledge_explain` also ignored `repo` scope and reported corpus-wide coverage under a repo-scoped request | Caps final <95; −categories 3 and 6 |
| 19. Evaluator uses source, DB, Git, terminal, CLI, old-report, or another-index fallback | PASS | None used. Two host utilities disclosed and applied only to MCP output: `date -u` for one wall-clock stamp, `jq` over the `knowledge_capabilities` MCP response the host spooled to a local file. No corpus file, Git command, SQLite query, Penguin CLI invocation, prior report, or other index product was touched. Report **not** marked INVALID | none |

**Hard gates: 15 PASS, 4 FAIL** (gates 2, 7, 16, 18). Two additional PASS-with-caveat (9, 15).

## 7. Category scores and weighted arithmetic

| Category | Weight | Category score /100 | Weighted contribution | Evidence summary | Remaining gap |
| --- | ---: | ---: | ---: | --- | --- |
| 1. Runtime identity, installed MCP parity, reliability | 10% | 62 | 62 × 10 / 100 = **6.20** | Complete, self-consistent identity chain: session, connect time, app 1.16.0, build `1.16.0-f5de64b02a83a582` running==available, `restartRequired:false`, schema 18, contract 2, capabilityHash + modelHash echoed on every response; DB connected at schema 18; native deps all `ready` with sha256 | 9 hard timeouts in one session (endpoints ×5, semantic-hybrid search ×3, bogus-target flow ×1); 21 s architecture; 1.9× latency variance on identical input; `indexedSchemaVersion 17` vs runtime 18 unreconciled |
| 2. Discoverability, schemas, annotations, onboarding, remediation | 14% | 80 | 80 × 14 / 100 = **11.20** | 101-capability manifest with machine-readable `mutating`/`confirmation`/`supportsCursor`/`supportsCompact`/4 annotation hints and **zero** inversions across 39 mutating + 62 read-only; remediation present and actionable on all 8 typed root/cursor/target/scope error classes; onboarding uses correct nested scope/options/page shapes and correct cursor-continuation guidance | Onboarding ships two non-existent tool ids (`knowledge_get_node`, `knowledge_architecture`) and an unreconciled endpoint count (20 vs 608); no tool-id↔capability-id alias map; `destructiveHint:true` on additive ops and `openWorldHint:false` on install/sync are imprecise; `QUERY_TIMEOUT` has no remediation; `options.explain` accepted with no effect |
| 3. Exact/lexical/graph/context/flow usefulness and latency | 20% | 68 | 68 × 20 / 100 = **13.60** | Exact/phrase/substring/path/symbol lanes give byte-exact verified provenance with contentHash; 4-page occurrence walk to deterministic exhaustion without collapsing 4 same-file occurrences; context honors a global limit of 2 across 3 pages with no drift; flow reaches a persistence boundary at depth 4 with `evidenceState:"proven"` + `confidence:1` + file:line on every hop | Latency is the dominant weakness: 2.5–14 s typical, 21 s architecture, endpoints unusable, 13 s for a 9-word query; `scope.kinds` silently zeroes valid results; `knowledge_explain` ignores `repo`; class symbols unreachable by name (`PromotionRedisService` resolves only to field nodes); flow `truncated:true` with no cursor |
| 4. Semantic readiness, model identity, hybrid honesty, lifecycle | 16% | 44 | 44 × 16 / 100 = **7.04** | Honesty is genuinely good: semantic is never claimed applied, `MODE_UNAVAILABLE`/`no_active_space` is typed, `skippedLanes` and `SEMANTIC_LANE_UNAVAILABLE` are explicit, `ready:0/expected:0/activeGenerationIds:[]` on every call, and `semantic_status` never invents an active generation | Zero generations exist anywhere; `semantic_status` returns a bare `{"statuses":[]}` exposing **none** of the required lifecycle fields (counts, provider/model/revision, dimensions, digests, pooling, normalization, chunker, worker lease/heartbeat/ETA); hybrid/fallback orchestration exceeds the hard budget; ~10.5 s dead overhead per semantic-requested call; per-generation model identity unavailable; B3/B4/B6 all unexecutable |
| 5. Stable identities, pagination, cursor correctness, affected parity | 20% | 74 | 74 × 20 / 100 = **14.80** | Identity round trip exact in both directions (nodeId **and** identityKey), with path/kind/lines/contentHash stable across 5 capabilities; search/files/context/coverage pagination exact with no overlap or drift and deterministic exhaustion; wrong-family and tampered cursors return **distinct** typed codes with remediation; file-scoped and symbol-scoped affected are byte-identical | `knowledge.endpoints` advertises cursor support it cannot deliver (hard gate 7); `flow` reports `truncated:true` with `cursor:null`; symbol-scoped affected collapses to file granularity; `impactEdges:[]` leaves the impacted node unexplained; `candidateCount` is limit-dependent; `saved_query.run` cursor untestable, `file_symbols`/`note.list` untested |
| 6. File reconciliation, coverage debt, cache hygiene, negative-proof honesty | 15% | 84 | 84 × 15 / 100 = **12.60** | Reconciliation block publishes three equations and all three hold with `totalIsExact:true`; ≥3 source-only samples explained; two non-zero coverage families paged with concrete reasonCodes, exact `remainingCount` arithmetic and `reconciliation delta 0`; `failed:0` matches an empty exact page; all five cache patterns plus `node_modules/` clean with explicit `EXCLUDED_FILES_MATCH_PATH` metadata; every empty result carries `NO_MATCH_INCOMPLETE` + `COVERAGE_INCOMPLETE` + coverage `nextActions`, and unscoped calls carry `GLOBAL_SCOPE_CAPPED` | `architecture.nodeCounts.file 3306` vs `graphParsedFiles 3298` unreconciled; endpoint 608 vs 20 unreconciled and unverifiable while endpoints times out; the `scope.kinds` silent false negative is a negative-proof failure the machinery does not annotate; `knowledge_explain` reports corpus-wide coverage under a repo-scoped request |
| 7. Compactness and Agent handoff usability | 5% | 80 | 80 × 5 / 100 = **4.00** | Compact preserves canonical items, non-zero counts, meaning, continuation and caveats with real reduction (dead_code 0.603, endpoints 0.622/0.646, flow 0.540); byte fields honestly named `…Estimate` so no impossible exactness is claimed; Agent B resumed from an MCP-emitted scope + identity + one unused cursor with no rediscovery and no drift | `knowledge_context` repeats the full ~5.5 KB focus source on **every** page while enforcing a 2-item budget; `knowledge.search` advertises `supportsCompact` but yields `compactRatio 1`; normal-vs-compact endpoints uncomparable because the non-compact call times out; the serialization boundary (pre-transport JSON, excluding MCP framing) is not documented in the response |

**Weighted arithmetic:**

```
6.20 + 11.20 + 13.60 + 7.04 + 14.80 + 12.60 + 4.00 = 69.44
```

- **Raw weighted score: 69.44 / 100** (reported as **69/100**; not rounded upward)
- **Hard-gate-adjusted final score: 69 / 100** — four hard gates fail (2, 7, 16, 18), so the ceiling of 95 applies; the raw score already sits well below that ceiling, so the cap is not the binding constraint.

## 8. Verdicts by dimension

| Dimension | Verdict | Basis |
| --- | --- | --- |
| Accuracy | **GOOD** | Every positive claim carried repo + branch + commitSha + snapshotId + stable identity + file:line (+ byte range + contentHash for source occurrences). No fabricated hop: all 20 flow hops were `evidenceState:"proven"`, `method:"EXTRACTED"`, `confidence:1`. Identity round trip exact. No cross-repo or cross-branch leakage in any scoped call. |
| Recall | **PARTIAL** | Forward checks succeeded (intent → controller/service/module/repository, exact identifier → all 8 occurrences). Reverse checks exposed two holes: `scope.kinds:["symbol"]` returns nothing for a symbol the unfiltered query finds, and a class named in `architecture.hubs` is unreachable by name (only its field nodes resolve). 95718 unresolved references bound all graph recall to a lower bound. |
| Evidence quality | **STRONG** | contentHash + `status:"verified"` per source hit; `graphEvidence` with origin/method/confidence/scope/provenance per edge; `completeness`, `proofStatus`, `totalIsExact`, `gaps`, `truncated` on essentially every response; `untrustedContent:true` on retrieved text. `affected.tests:[]` rather than filename inference is a notable integrity win. |
| Completeness | **PARTIAL** | File reconciliation and coverage debt are exact and pageable. Endpoint inventory is not enumerable in the largest repo; two count pairs (3306/3298 files, 608/20 endpoints) are unreconciled; `flow` truncates with no continuation. |
| Reliability | **WEAK** | 9 `QUERY_TIMEOUT`s in one session. `knowledge_endpoints` failed 5 of 7 calls, including continuing its own fresh cursor, and succeeded only at 27.9 s and 12.5 s. Identical inputs varied 1.9× in latency. Failure is size-correlated but not size-predictable: an 85-file repo timed out while a 75-file repo answered in 12.5 s. |
| Latency | **WEAK** | Median heavy call 4–14 s against a 30 s hard timeout, so the usable margin is thin. `get_architecture` 21.0 s. Any semantically-blended multi-term query exceeds the budget. Only `get_node` (126 ms) and the doctor preflights (2–8 ms) are fast. |
| Semantic/vector readiness | **NOT READY** | No generation exists in any scope. `semantic_status` exposes no lifecycle, model, dimension, digest, pooling, chunker or worker fields. Requesting semantic assistance actively degrades deterministic retrieval past the hard budget. |
| First-user MCP usability | **CONDITIONAL** | A first-time MCP-only consumer can genuinely investigate: onboarding is concrete and correctly shaped, remediation is actionable without source/DB/CLI, and an Agent handoff resumes from MCP-emitted state alone. They will however hit two copy-paste-broken tool ids in the onboarding output, an endpoints capability that mostly times out, a silent false negative from `scope.kinds`, and no working semantic discovery. |

## 9. Every hard-gate failure

1. **Gate 2 — index/runtime schema conflict.** `trust.indexedSchemaVersion 17` against `trust.schemaVersion 18` on every `knowledge_context` / `knowledge_flow` response for FPMS-NT. Build and contract parity are otherwise clean. The skew is disclosed but carries no typed status, no reconciliation, and no remediation, so an MCP-only consumer cannot tell whether any capability is degraded by it.
2. **Gate 7 — advertised cursor support undeliverable.** `knowledge.endpoints` declares `supportsCursor:true`, emits a syntactically valid `nextCursor` (FPMS-NT-Provider page 1, 27894.07 ms), and then fails `QUERY_TIMEOUT` when that exact cursor is passed back. No endpoints continuation succeeded in any of the 5 repositories attempted.
3. **Gate 16 — graph/lexical blocks on semantic work.** The same multi-term query completes in 13120.447 ms with `options.semantic:"off"` and times out with `"blend"` (twice) and `"fallback"` (once). The single-term probe isolates ~10.5 s of semantic orchestration charged outside the measured query (outer 12790.265 ms vs inner 2273.823 ms) while `activeGenerationIds` is empty — the deterministic lanes are paying for a lane that has nothing to contribute.
4. **Gate 18 — a product-generated negative that ignores its own limit.** `knowledge_search` with `scope.kinds:["symbol"]` returns `candidateCount 0` / `NO_MATCH_INCOMPLETE` for `VaultFetcher`, while the identical query without `kinds` returns it with a verified graph nodeId and `candidateCount 836`. The response asserts `scopeApplied:true` and emits **no** warning that a scope filter eliminated every candidate, so the negative looks well-formed and is wrong. Compounded by `knowledge_explain` silently discarding `repo` and reporting corpus-wide coverage (`discovered 33300`) under a repo-scoped request.

Additional brief requirement failed outside §10: **Q15 step 4** — `mutation_preflight` with a malformed `root_path` must return `MUTATION_PREFLIGHT_INVALID` (or be blocked by schema validation). Instead `root_path:"not-an-absolute-path"` returned `status:"valid"`, was silently CWD-joined to `/Users/shieng/not-an-absolute-path`, and produced a canonical `nextAction` with `confirmed:true` against a non-existent, never-owner-approved root.

## 10. Five highest-priority remaining defects

| # | Defect | Reproducible MCP evidence | Impact |
| ---: | --- | --- | --- |
| 1 | **`knowledge_endpoints` is effectively unusable and its advertised cursor cannot be continued.** | `knowledge_endpoints({repo:"FPMS-NT", limit:4, handled_only:true})` → `QUERY_TIMEOUT`; `{repo:"FPMS-NT", limit:3}` → `QUERY_TIMEOUT`; `{repo:"FPMS-NT-Risk-Control", limit:2, compact:true, handled_only:true}` → `QUERY_TIMEOUT`; `{repo:"FPMS-NT-Recommend", limit:1, compact:true}` → `QUERY_TIMEOUT`; `{repo:"FPMS-NT-Provider", limit:1, compact:true, protocol:"grpc"}` → OK at 27894.07 ms with `nextCursor`; passing that `nextCursor` back → `QUERY_TIMEOUT`; `{repo:"FPMS-Proposal-SDK", limit:1, compact:true}` → OK at 12520.016 ms | Breaks hard gate 7 and Q12; makes endpoint inventory, `handled_only` triage, and the 608-vs-20 endpoint count discrepancy all unverifiable in the largest repo |
| 2 | **Requesting semantic assistance breaks deterministic retrieval past the hard budget.** | `knowledge_search({query:"block further deposits once the player limit is reached", scope:{revisions:[{repoName:"FPMS-NT",branch:"brazil-v2"}]}, page:{limit:3}, options:{semantic:"off"}})` → MATCH, 13120.447 ms, `candidateCount 2875`. Same call with `options:{semantic:"blend"}` → `QUERY_TIMEOUT` (reproduced twice); with `options:{semantic:"fallback"}` → `QUERY_TIMEOUT`. Overhead isolated on a single-term query: outer `timingsMs.total 12790.265` vs `diagnostics.timingsMs.total 2273.823`, with `semantic{applied:false, reason:"no_active_space", activeGenerationIds:[]}` | Breaks hard gate 16 and blocks Q8; an agent that follows the natural "try hybrid" path loses the deterministic answer it would otherwise have got |
| 3 | **`scope.kinds` produces a silent false negative.** | `knowledge_search({query:"VaultFetcher", mode:"exact", scope:{revisions:[{repoName:"FPMS-NT",branch:"brazil-v2"}], kinds:["symbol"]}, page:{limit:2}})` → `candidateCount 0`, `queryStatus "NO_MATCH_INCOMPLETE"`, `searchedLanes ["source","symbol"]`, `scopeApplied:true`, no filter warning. Drop `kinds` → `candidateCount 836`, hit `node_0a4422f9-e8d5-44d0-bf35-ed9369615208` at `libs/tools/src/vault/vault-fetcher.ts:8`, `status:"verified"` | Breaks hard gate 18; a plausible narrowing option converts a present symbol into a confident "not found" that the negative-proof machinery does not flag |
| 4 | **Mutation preflight accepts a malformed root and emits a confirmation-ready action for it.** | `knowledge_doctor({mutation_preflight:{action:"rebuild", root_path:"not-an-absolute-path"}})` → `{status:"valid", readOnly:true, mutationPerformed:false, rootPath:"/Users/shieng/not-an-absolute-path", confirmationRequired:true, nextAction:{tool:"knowledge_rebuild", arguments:{root_path:"/Users/shieng/not-an-absolute-path", confirmed:true}, ownerApprovalRequired:true}}`. Contrast `root_path:"/System/Library"` → `ROOT_PATH_OUT_OF_SCOPE` with remediation | Fails Q15 step 4; the preflight's whole purpose is to validate an owner-local root before a destructive action, and it hands back a `confirmed:true` payload for a path that does not exist |
| 5 | **`knowledge_semantic_status` returns an empty array instead of a lifecycle projection.** | `knowledge_semantic_status()` and `knowledge_semantic_status({scopeKey:"repo:repo_a48ec7fb-5987-47df-9198-06969359cb50"})` → `{"statuses":[]}` on 5 reads spanning 07:33:30 → 08:10. No expected/ready/pending/running/retryable/terminal counts, no provider/model/revision, no dimensions, no weights/tokenizer/preprocessing digests, no pooling, no normalization, no chunker version, no worker lease/heartbeat/rate/ETA | Q7 cannot be evidenced beyond "nothing is falsely active"; B4 and B6 are unprovable; an owner cannot tell "never started" from "cleared" from "provider unavailable" |

Honourable-mention defects recorded above but outside the top five: onboarding emitting the non-existent tool ids `knowledge_get_node` and `knowledge_architecture`; `knowledge_explain` discarding `repo` scope; symbol-scoped `affected` collapsing to file granularity with `impactEdges:[]`; `flow.relatedTests` naming a test file with no edge evidence while `affected.tests` is correctly empty; `flow` reporting `truncated:true` with `cursor:null`; `context` repeating the full focus source on every budgeted page; `candidateCount` shrinking when a smaller page limit is requested; `QUERY_TIMEOUT` carrying no remediation; two repositories sharing the name `FPMS-NT-Auth-Player`; `architecture.nodeCounts.file 3306` vs `graphParsedFiles 3298`.

## 11. Final verdict

**NO-GO.**

Round 26 shows a product whose *evidence discipline* is genuinely strong — reconciliation equations that hold, coverage debt that is concrete and pageable, cache hygiene that surfaces exclusion metadata rather than hiding it, a capability manifest with zero annotation inversions across 101 entries, exact identity round trips, distinct typed codes for wrong-family versus tampered cursors, and a negative-proof convention that refuses to let an empty result read as proof of absence. Several Round 25 complaints are visibly fixed: the file-count reconciliation is now published and correct, MCP safety annotations are complete and truthful, owner-mutation discovery works with no mutation and no permission prompt, and cursor/root failures are typed and carry actionable remediation.

It fails acceptance on *delivery*, not on honesty. Four hard gates fail: an index/runtime schema skew that is disclosed but unreconciled; an endpoints capability that advertises cursor support it cannot deliver and times out on 5 of 7 calls; semantic orchestration that drags deterministic graph and lexical retrieval past the 30-second budget while having zero embeddings to contribute; and a `scope.kinds` filter that manufactures a confident false negative the negative-proof machinery does not catch. A fifth serious problem sits just outside the gate list: the mutation preflight hands back a `confirmed:true` rebuild action for a relative path it silently rewrote.

Raw weighted score **69.44/100**, hard-gate-adjusted final **69/100**. The Round 26 closure rule requires 95–100 with every hard gate passing, so the implementation stays open.

